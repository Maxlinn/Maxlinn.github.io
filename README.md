# Maxlinn.github.io

For content safety reasons, the actually blog posts were hidden.

And any request to `maxlinn.github.io/{path}` will be redirected to `maxlinn.site/{path}`, which is served elsewhere.
